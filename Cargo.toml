[package]
name = "zellij-ime-per-pane"
version = "0.1.0"
edition = "2021"

[dependencies]
zellij-tile = "0.44"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = true
panic = "abort"
