[package]
name = "zellij-ime-per-pane"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = "0.4"
zellij-tile = "0.44"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = false
codegen-units = 16
strip = true
panic = "abort"
